//now I want to see if I can use map to change the array
map(function(x) {return x + 's'}, food); {

	console.log(map);
}





module.exports.map = function(array, n, fn) {
	return n * n};

	string[string.length-1]
